# Mr. Robot MCP server — runtime dependencies.
# mcp and PyYAML already ship with Kali's system Python; listed for portability.
mcp>=1.22
PyYAML>=6.0

# Cross-engagement memory layer (ADR-0014). Not yet on PyPI — install from
# the repo. If absent, server/memory.py degrades to a no-op and the rest of
# the system runs unchanged.
#   pip install --user --break-system-packages git+https://github.com/ry-ops/aiana.git
# aiana >= 1.2.1
